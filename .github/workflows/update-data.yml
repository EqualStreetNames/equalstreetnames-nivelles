name: Update data

on:
  schedule:
    - cron: "30 5 1 * *"
  push:
    paths:
      - ".github/workflows/update-data.yml"
      - "config.php"
      - "overpass/*"
  pull_request:
    paths:
      - ".github/workflows/update-data.yml"
      - "config.php"
      - "overpass/*"

jobs:
  update-data:
    uses: EqualStreetNames/equalstreetnames/.github/workflows/reusable-update-date.yml@master
    secrets: inherit
    with:
      city: belgium/nivelles
